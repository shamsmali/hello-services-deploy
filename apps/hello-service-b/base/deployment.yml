apiVersion: apps/v1
kind: Deployment
metadata:
  name: hello-service-b
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: hello-service-b
  template:
    metadata:
      labels:
        app: hello-service-b
    spec:
      containers:  
        - image: shamsmali/hello-service-b:2.0.3
          name: hello-service-b
          ports:
            - containerPort: 8080
          readinessProbe:
            httpGet:
              path: /ping
              port: 8080
          livenessProbe:
            httpGet:
              path: /ping
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5