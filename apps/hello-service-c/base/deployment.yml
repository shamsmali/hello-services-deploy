apiVersion: apps/v1
kind: Deployment
metadata:
  name: hello-service-c
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: hello-service-c
  template:
    metadata:
      labels:
        app: hello-service-c
    spec:
      containers:  
        - image: shamsmali/hello-service-c:2.0.2
          name: hello-service-c
          ports:
            - containerPort: 8080
          readinessProbe:
            httpGet:
              path: /ping
              port: 8080
          livenessProbe:
            httpGet:
              path: /ping
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5